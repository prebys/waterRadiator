# waterRadiator
Water-based Cerenkov detector for Mu2e diagnostic line extinction monitor

Based on the geant4 example B1.  To build and use:

* Create a parallel directory called waterRadiator-build
* cd waterRadiator-build
* cmake ../waterRadiator
* make
